# Add directories containing individual modules.

# Enable or disable the build of each app by commenting its respective line.
add_subdirectory(configurator)
add_subdirectory(influx)
add_subdirectory(memdb)
add_subdirectory(minix)